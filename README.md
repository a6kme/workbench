### Requirements
1. `docker` should be installed on the host machine
1. Assuming the IDE to be Visual Studio Code

### Workflow for Backend and Frontend
1. Once a project has been created with copier, the project should be opened as a dev container in vs code
1. The container comes pre configured with required extensions, python and nvm
1. Any extra service should be started in the dev container by calling docker compose up
